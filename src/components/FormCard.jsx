import React from "react";

const FormCard = () => {
  return (
    <div>
      <form action="">
        <input type="image" placeholder="Add Profile Image" />
        <input type="text" placeholder="Add your profile name" />
        <input type="text" placeholder="write your comment here" />
      <button>Add Comment</button>
      </form>
    </div>
  );
};

export default FormCard;
